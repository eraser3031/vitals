# Vitals — 작업 규칙

## UI / 카피

- **앱 UI, 문자열, 소스 코드에 이모지를 넣지 않는다.** 사용자가 명시적으로 요청할 때만 예외.
  - 아이콘이 필요한 자리에는 `lucide-react`를 쓴다 (기존 코드 컨벤션).
  - 모드/상태/카테고리 같은 분류는 텍스트 라벨 또는 배지(`bg-primary-light text-primary` 등)로 표현한다.
  - `electron/main`에서 생성하는 마크다운·문자열(진단 컨텍스트, 타임라인 등)에도 이모지를 넣지 않는다.
